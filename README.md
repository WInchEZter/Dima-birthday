
# Dima Birthday — Vercel Ready 🎪💡

Комбо-стиль: цирк + неон, конфетти/фейерверки (React + Vite + Framer Motion).

## Что сделать перед деплоем
1) Добавь свой трек в `public/music.mp3` (имя файла строго `music.mp3`).
2) При желании замени фото: `public/dima1.jpg` и `public/dima2.jpg`.

## Локально
```bash
npm install
npm run dev
```
Открой ссылку из терминала (обычно http://localhost:5173).

## Деплой на Vercel
1) Залей проект в новый GitHub репозиторий.
2) На https://vercel.com → **Import Project** → выбери репозиторий → **Deploy**.
3) Готово! Получишь ссылку вида `https://dima-birthday.vercel.app`.

## Структура
- `public/` — статика (фото, музыка).
- `src/App.jsx` — логика и анимации.
- `src/styles.css` — стили.
- `vite.config.js`, `vercel.json`, `package.json` — конфигурация для Vercel.
